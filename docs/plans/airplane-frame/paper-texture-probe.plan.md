# Paper luggage-tag texture probe

**Status:** IMPLEMENTED (explore mock shipped 2026-08-04). Explore only — production ship tracked in [paper-texture-ship.plan.md](./paper-texture-ship.plan.md).

## Entry

Start here: this plan. Then load the set below.

## Required reading

- [docs/agents/design-mock-probe-pointer.md](../agents/design-mock-probe-pointer.md) — `mock_dir`, authority order
- [docs/design-mocks/README.md](../../design-mocks/README.md) — inventory + local HTTP serve
- [docs/plans/airplane-frame/visual-direction.md](visual-direction.md) — locked panel ground/ink/tag rules to **preserve** (do not amend locks)
- [docs/design-mocks/poster-ad-wall.html](../../design-mocks/poster-ad-wall.html) — visual base to **fork from** (do not edit this file)

## Prerequisites

- Branch: any; explore mock only
- Env: local static HTTP
- Tools: browser + DevTools; `python3 -m http.server` (or equivalent)
- User gates: none (explore-only; no lock pass)

## Locked decisions (this probe)

| Topic | Lock |
|-------|------|
| Mode | Explore only — no `visual-direction` lock, no ship to Pages app |
| Technique | Hybrid: 1–2 photographic paper bases + per-panel SVG uniqueness |
| Dial | CSS custom properties on `:root` only (no mock sliders) |
| Shape | Texture on current rectangular panels only |
| Deliverable | Single explore HTML mock |

## Deliverables

| Artifact | Path |
|----------|------|
| Explore mock | [docs/design-mocks/paper-luggage-tag-texture.html](../../design-mocks/paper-luggage-tag-texture.html) |
| Paper textures | [docs/design-mocks/textures/](../../design-mocks/textures/) |
| Attribution | [docs/design-mocks/textures/ATTRIBUTION.md](../../design-mocks/textures/ATTRIBUTION.md) |
| Texture generator | [docs/design-mocks/textures/gen-paper.py](../../design-mocks/textures/gen-paper.py) |

## Dial knobs (`:root` in mock)

- `--paper-opacity` — overall paper layer strength (0–1)
- `--paper-wrinkle` — warp/skew intensity on photo layer (0–1)
- `--paper-wear` — contrast/aging on photo layer (0–1)

## Verify

```bash
python3 -m http.server 8765 --directory docs/design-mocks
# open http://localhost:8765/paper-luggage-tag-texture.html
```

- Eight rectangular panels with distinct `data-paper-slot` crop + SVG warp
- DevTools tweak of `--paper-*` visibly changes intensity
- Carrier color and ink contrast remain readable

## Out of scope

- Shipping texture into `index.html` / `css/poster.css` / production assets
- Locking decisions in `visual-direction.md`
- Editing canonical `poster-ad-wall.html`
- Mock intensity sliders / product chrome
- Phase 6 poster polish UAT
- Plan-build HANDOFF “next action” rewrite (backlog spike stays exploratory until a later lock pass)

## UAT (smoke)

- Open mock over local HTTP on desktop and ~375px width
- No obvious identical texture tiling across adjacent panels
- Paper reads vintage luggage tag; dial down via CSS vars to near-subtle
- Text/ink contrast usable on branded grounds

**Last updated:** 2026-08-05
