{
  "name": "airplane-frame-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start:pi": "node src/node/server.js"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
